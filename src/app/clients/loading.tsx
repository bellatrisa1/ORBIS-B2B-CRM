export default function Loading() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        minHeight: '60vh',
      }}
    >
      <p style={{ fontSize: '18px', color: '#8b929e' }}>Загрузка...</p>
    </div>
  );
}
